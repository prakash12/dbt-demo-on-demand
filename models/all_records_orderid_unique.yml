version: 2

models:
  - name: all_records
    columns:
       - name: order_id
         tests:
           - unique
           - not_null
       - name: order_priority
         tests:
           - accepted_values:
               values:
                 - L
                 - H
                 - M
                 - C

              

